[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Redirect all routes to index.html (needed for React Router / SPAs)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
